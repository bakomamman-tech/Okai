# Okai Client

The `server/client` app is the active frontend for Okai.

## Scripts

- `npm install`
- `npm run dev`
- `npm run build`

The Vite dev server proxies `/api` and `/uploads` to `http://localhost:5000`.
